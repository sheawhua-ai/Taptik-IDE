The `ExecutionTask` interface in `src/components/merchant/ExecutionCenter/types.ts` is missing the following specific fields required for the new logic, as per your instructions:

1. **任务形态 (Task Format)**: Missing a dedicated field for `笔记包` | `任务包` | `自有指定`. Currently, it uses `publishType` ('自有员工发布', 'KOS店长发布', 'KOC协作发布').
2. **领取人/被指派员工及时间**: Needs `assigneeName` (领取人/被指派员工), `claimTime` (扫码领取时间/员工打开任务时间).
3. **问卷状态与时间 (Questionnaire)**: Missing `questionnaireStatus` and `questionnaireTime`.
4. **素材提交状态与时间 (Photos)**: Missing `photoMaterialStatus` and `photoMaterialTime`.
5. **笔记成稿状态**: Missing `noteFinalizedStatus`.
6. **额度占用 (Quota)**: Missing quota information (e.g., `quota: { claimed: number, total: number }`).

The current `publishStage` ('待通知' | '待领取' | '待发布' | '已回传' | '识别中' | '观察中') might partially cover publish/return status, but doesn't track specific times or the detailed task package intermediate steps (questionnaire, photo).

Should I add these exact fields to the `ExecutionTask` interface in `types.ts` and populate them in `mockData.ts` to proceed?
