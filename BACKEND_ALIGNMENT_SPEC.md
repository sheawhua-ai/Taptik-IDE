# TAPTIK 前后端功能对齐清单

> 文档用途：供产品、前端、后端、测试逐项对齐，避免只实现页面接口而遗漏状态、异步任务、通知、权限、证据与数据回流。
>
> 当前基线：前端核心数据均为本地 Mock / React state，尚未接入真实业务 API。本文描述的是后端目标能力，不代表现有后端已经实现。

## 0. 阅读与验收约定

- `P0`：首个可用闭环必须具备；缺失会导致主流程中断。
- `P1`：提高可运营性、可追溯性或自动化程度；可在 P0 后交付。
- `P2`：扩展能力或未来渠道。
- 每一个功能点只有同时满足“接口可用、状态正确、权限正确、刷新后可恢复、异常可重试、关键操作有审计”才算完成。
- 前端不再负责生成业务 ID、推进真实状态、伪造时间线或把缺失数据写成 `0`。
- 所有时间使用 ISO 8601，数据库统一存 UTC，接口同时返回时区或由前端按用户时区显示。
- 所有写接口必须校验商家归属、角色权限、资源版本；创建类接口支持 `Idempotency-Key`。

## 1. 业务全景与数据归属

```text
组织 Organization
├─ 成员 User / Role / Permission
├─ 商家 Merchant
│  ├─ 商家档案 MerchantProfile
│  ├─ 新商家起盘 LaunchGuide / LaunchTemplate / LaunchProgress
│  ├─ 知识来源 KnowledgeSource（本地文件或目录链接）
│  │  └─ 原子知识 KnowledgeFact / 冲突与待确认任务
│  ├─ 素材 MaterialAsset / MaterialCollectionTask
│  ├─ 账号 AccountAsset / EmployeeBinding / DeviceBinding
│  ├─ 方案 Project
│  │  ├─ 策略版本 StrategyVersion
│  │  ├─ 内容位 NoteSlot / ContentDraft
│  │  ├─ 素材需求 MaterialRequirement / MaterialTask
│  │  ├─ 发布任务 PublishTask
│  │  ├─ 已发布笔记 PublishedNote / 数据快照
│  │  ├─ 异常 Issue / 待办 ActionTask / 时间线 TimelineEvent
│  │  └─ 复盘 Review / 调整建议 ReviewAdjustmentProposal
│  └─ 对话空间 Conversation / Message / AgentRun
└─ 用量与账单 UsageLedger / Subscription
```

核心归属规则：

1. 所有业务资源必须携带 `organizationId` 和 `merchantId`；方案内资源还必须携带 `projectId`。
2. 方案生成的内容、素材需求和发布任务必须冻结所使用的 `strategyVersionId`，后续改策略只影响新生成内容。
3. 员工 H5 与外部发布 H5 只能访问被授权的单个任务，不能凭二维码枚举商家、方案或其他任务。
4. 本地知识资料不上传原文件到云端；后端保存来源登记、索引结果、摘要、哈希、授权状态和同步状态。若产品未来允许上传云端，必须作为另一种来源类型单独实现。
5. 指标缺失时返回 `null` 和缺失原因，不得返回 `0` 冒充真实数据。

## 2. 通用接口规范（所有模块共用）

### 2.1 建议基础协议

- API 前缀：`/api/v1`
- 鉴权：管理端使用用户登录令牌；员工 H5 使用员工会话；外部 H5 使用短期、签名、限任务令牌。
- 分页：`cursor` + `limit`，响应 `items`、`nextCursor`、`hasMore`。
- 乐观锁：可修改资源返回 `version`；更新时传 `If-Match` 或请求体 `version`，冲突返回 `409 RESOURCE_VERSION_CONFLICT`。
- 幂等：创建商家、生成方案、创建任务、认领、提交回执等写操作接受 `Idempotency-Key`。
- 异步任务：AI 生成、资料解析、素材分析、账号采集、发布识别、复盘生成统一返回 `jobId`，状态为 `queued/running/succeeded/failed/cancelled`。
- 实时更新：优先 SSE；也可 WebSocket。至少支持轮询 `GET /jobs/{jobId}`。
- 统一错误：`code`、`message`、`fieldErrors`、`requestId`、`retryable`、`details`。
- 删除：默认软删除，返回 `deletedAt/deletedBy`；被方案、发布或复盘引用的数据不可直接物理删除。

### 2.2 所有写操作的审计字段

`createdAt`、`createdBy`、`updatedAt`、`updatedBy`、`version`、`source`、`requestId`。涉及 AI 时再记录 `model`、`promptVersion`、`inputSnapshotId`、`outputSnapshotId`、`tokenUsage`；涉及人工确认时记录确认人、确认时间和确认意见。

## 3. 身份、组织、权限与商家隔离

### 功能清单

- [ ] **P0 用户登录与会话**：登录、刷新令牌、退出、会话失效、设备列表。
- [ ] **P0 组织成员**：成员列表、邀请、停用、角色调整；停用成员不能继续处理任务。
- [ ] **P0 商家级授权**：成员可被授权全部商家或指定商家。
- [ ] **P0 数据隔离**：任何 ID 查询都必须同时验证组织与商家归属，禁止跨商家越权。
- [ ] **P0 操作权限**：至少区分组织管理员、商家管理员、操盘手、内容审核员、素材执行人、发布员工、只读成员。
- [ ] **P1 SSO 与企业身份**：保留企业单点登录配置、域名验证和登录策略。
- [ ] **P1 审计日志**：按成员、商家、资源、动作、时间查询和导出。

### 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/me` | 当前用户、组织、角色、可访问商家 |
| `GET/POST` | `/organizations/{orgId}/members` | 查询/邀请成员 |
| `PATCH` | `/organizations/{orgId}/members/{userId}` | 角色、状态、商家范围 |
| `GET` | `/audit-logs` | 审计检索 |

## 4. 新增商家

### 4.1 创建方式与字段

- [ ] **P0 手动创建**：商家名称、负责人账号、手机号、初始密码、一级行业、二/三级行业标签。
- [ ] **P0 字段校验**：商家名必填且组织内不重复；手机号格式；密码至少 8 位；一级行业必填；行业编码必须来自行业字典。
- [ ] **P0 原子创建**：一次事务创建商家、商家档案、行业默认起盘模板副本、起盘进度、默认权限；任何一步失败均回滚。
- [ ] **P0 行业默认值**：后端根据行业编码返回推荐策略、阶段、内容方向、默认账号角色、建议资料和建议素材，不允许只在前端写死。
- [ ] **P0 创建后跳转**：响应明确返回 `nextRoute: new_merchant_launch`、`merchantId` 和 `launchGuideId`；前端切换到该商家并进入起盘导航。
- [ ] **P0 新商家标识**：`onboardingStatus=new/in_progress/completed/skipped`；只有新建商家显示默认起盘导航。
- [ ] **P0 初始账号交付**：若创建负责人登录账号，密码只用于首次创建且不可回传；支持首次登录强制改密。
- [ ] **P1 邀请二维码创建**：按行业生成邀请链接/二维码，具有过期时间、使用次数和撤销能力。
- [ ] **P1 邀请落地创建**：受邀者补充资料后创建或加入商家；重复手机号/重复商家需有合并或提示策略。

### 4.2 建议接口

| 方法 | 路径 | 关键请求/响应 |
|---|---|---|
| `GET` | `/industry-catalog` | 多级行业树、版本、启停状态 |
| `GET` | `/industry-catalog/{code}/launch-defaults` | 行业默认模板预览 |
| `POST` | `/merchants` | 创建商家；响应含起盘导航 ID 和下一路由 |
| `GET` | `/merchants` | 搜索、状态、行业、负责人筛选 |
| `GET` | `/merchants/{merchantId}` | 商家摘要、权限、起盘状态 |
| `POST` | `/merchant-invitations` | 创建邀请链接/二维码 |
| `POST` | `/merchant-invitations/{token}/accept` | 验证并接受邀请 |

### 4.3 创建商家请求建议

```json
{
  "name": "特唯普宠物食品",
  "owner": { "username": "operator01", "phone": "13800000000", "initialPassword": "********" },
  "industry": { "primaryCode": "pet", "secondaryCodes": ["pet_food"], "tertiaryCodes": ["puppy_food"] },
  "source": "manual"
}
```

### 4.4 后端验收

- [ ] 刷新页面后新商家、行业、起盘状态仍存在。
- [ ] 两次相同幂等键只产生一个商家。
- [ ] 创建失败不留下孤立账号、模板或进度。
- [ ] 非授权成员无法看到或切换到该商家。
- [ ] 完成或跳过起盘后，旧商家常规页面不再自动显示新商家导航。

## 5. 新商家起盘导航

> 此页面只负责“告诉操盘手下一步做什么”和跳转，不展示当前运营逻辑、运营数据或复盘结果。

### 5.1 模板与进度

- [ ] **P0 行业模板实例化**：创建商家时复制一份模板快照，后续行业公共模板升级不得悄悄改变已创建商家。
- [ ] **P0 七步导航**：确认商家档案 → 链接知识资料 → 准备首批素材 → 配置账号和员工 → 创建首个方案 → 进入发布执行 → 数据回流后复盘。
- [ ] **P0 非强制顺序**：前四步可任意开始；依赖不满足时展示原因，不伪造后续任务。
- [ ] **P0 状态持久化**：每步 `not_started/in_progress/completed/skipped/blocked`，保存完成来源、完成时间、操作者和关联资源 ID。
- [ ] **P0 自动完成**：例如成功链接知识来源后自动完成知识步骤；创建可用账号绑定后自动完成账号步骤。
- [ ] **P0 手动完成/撤销**：允许标记完成；若依赖资源删除或失效，后端可回退并记录原因。
- [ ] **P0 收起与跳过**：收起只属于个人 UI 偏好；跳过起盘属于商家业务状态，两者不能混为一谈。
- [ ] **P0 依赖门槛**：尚未生成方案内容时，不出现“查看发布任务”的空入口；无发布数据时，不出现“复盘任务”的空入口。
- [ ] **P0 发布步骤生成条件**：方案确认且产生至少一个发布任务后，步骤 6 才可执行。
- [ ] **P0 复盘步骤生成条件**：至少一条发布内容进入已验证/观察中且有可用数据或到达观察窗口，步骤 7 才可执行。
- [ ] **P1 下一步建议**：根据未完成步骤、阻塞项、商家资源状态计算，不由前端固定字符串决定。

### 5.2 自定义模板

- [ ] **P0 商家模板可编辑**：标题、说明、步骤顺序、是否启用、完成条件说明、目标模块。
- [ ] **P0 恢复行业默认**：恢复时生成新版本，不覆盖历史完成记录。
- [ ] **P0 模板版本**：保存 `baseIndustryTemplateVersion` 和 `merchantTemplateVersion`。
- [ ] **P1 另存为组织模板**：管理员可把商家模板保存为组织复用模板。

### 5.3 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/merchants/{merchantId}/launch-guide` | 模板、步骤、进度、下一步建议、依赖 |
| `PATCH` | `/merchants/{merchantId}/launch-guide` | 收起/跳过/恢复起盘 |
| `PATCH` | `/merchants/{merchantId}/launch-guide/steps/{stepKey}` | 手动完成、跳过、重新打开 |
| `PUT` | `/merchants/{merchantId}/launch-template` | 保存商家自定义模板 |
| `POST` | `/merchants/{merchantId}/launch-template/restore` | 恢复行业模板新版本 |

## 6. 商家档案

- [ ] **P0 基础档案**：名称、Logo、品牌介绍、行业、经营区域、联系人、状态。
- [ ] **P0 商家画像**：目标人群、核心产品、价格带、卖点、竞品、渠道、内容偏好、禁区。
- [ ] **P0 完整度**：后端按必填规则计算档案完整度和缺失字段，前端只展示结果。
- [ ] **P0 状态管理**：`active/suspended/archived`；归档前检查未完成任务并给出影响清单。
- [ ] **P1 行业变更**：保留历史；行业变化仅给出模板迁移建议，不自动覆盖既有方案和知识。
- [ ] **P1 文件树与历史对话迁移**：若保留旧结构，必须以商家 ID 归属，不将其塞入商家详情大对象一次性返回。

接口：`GET/PATCH /merchants/{merchantId}/profile`、`GET /merchants/{merchantId}/profile/completeness`、`POST /merchants/{merchantId}/archive`。

## 7. 方案中心（商家运营）

### 7.1 方案生命周期

- [ ] **P0 方案列表**：按全部、准备中、运行中、已归档筛选；支持搜索、游标分页。
- [ ] **P0 新建方案**：名称、目标、起止日期、预算、方案类型、目标账号与发布规模。
- [ ] **P0 方案状态**：建议统一为 `draft/preparing/running/paused/completed/archived/cancelled`，由后端校验合法迁移。
- [ ] **P0 方案详情**：概览、内容与素材、当前策略版本、发布结构、关键异常摘要。
- [ ] **P0 编辑与归档**：运行中的关键字段变更要记录影响；有未完成任务时禁止直接删除。
- [ ] **P0 批量生成运营方案**：一次事务/工作流生成策略、内容位、草稿、素材需求与必要任务；支持失败重试且不重复。
- [ ] **P0 内容位与发布任务分离**：内容位是“计划发布什么”，发布任务是“由谁在哪个账号执行”。方案尚未确认时可以有草稿，但不应向员工发送发布任务。
- [ ] **P1 暂停/恢复**：暂停后不再派发新任务，但保留已发布内容的数据观察。

### 7.2 策略与版本

- [ ] **P0 策略字段**：人群、核心问题、解决方案、验证假设、继续/停止条件、关键词、观察周期。
- [ ] **P0 不可变版本**：`initial/expert_adjustment/review_applied`；新版本创建后旧版本为 `superseded`，历史内容仍引用旧版本。
- [ ] **P0 变更边界**：应用新策略只影响之后生成的内容和任务；已发布、已确认和历史数据不被改写。
- [ ] **P0 版本差异**：记录变更字段、变更原因、创建人、生效时间。
- [ ] **P1 回滚**：回滚不是修改旧版本，而是基于旧版本创建新的活动版本。

### 7.3 内容位与草稿

- [ ] **P0 内容位**：轮次、账号类型、目标账号、内容方向、计划日期、内容包规格。
- [ ] **P0 草稿状态**：`pending_generation/generated/pending_confirmation/confirmed/rejected`。
- [ ] **P0 生成与重生成**：单条或批量，保留每次生成版本、输入快照和人工编辑。
- [ ] **P0 人工编辑**：标题、正文、标签、合规结论；冲突时返回版本冲突而非覆盖。
- [ ] **P0 内容包反馈版本**：面向消费者/KOC 的内容包修改只影响后续领取，新旧领取记录必须可追溯。
- [ ] **P1 合规检查**：风险项、证据、建议修改、人工确认结果。

### 7.4 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET/POST` | `/merchants/{merchantId}/projects` | 列表/创建 |
| `GET/PATCH` | `/projects/{projectId}` | 详情/编辑 |
| `POST` | `/projects/{projectId}/transition` | 暂停、运行、完成、归档 |
| `POST` | `/projects/{projectId}/generate-full-plan` | 异步生成完整方案 |
| `GET/POST` | `/projects/{projectId}/strategy-versions` | 版本列表/创建新版本 |
| `GET/POST` | `/projects/{projectId}/note-slots` | 内容位列表/新增 |
| `POST` | `/projects/{projectId}/content-drafts:generate` | 批量生成草稿 |
| `GET/PATCH` | `/content-drafts/{draftId}` | 查看/编辑/确认草稿 |
| `POST` | `/content-drafts/{draftId}:regenerate` | 重生成并保留版本 |

## 8. 素材中心

### 8.1 素材资产

- [ ] **P0 素材入库**：图片/视频/组件，记录文件、缩略图、尺寸、时长、大小、哈希、来源、创建者。
- [ ] **P0 去重**：同商家内按文件哈希提示重复；保留用户明确选择。
- [ ] **P0 分类与用途**：发布素材、基础组件、衍生素材；封面、正文图、视频、实拍、Logo、包装、色卡等。
- [ ] **P0 状态**：`pending_acceptance/available/reserved/used/archived`；被引用素材不能物理删除。
- [ ] **P0 AI 分析**：描述、标签、画面元素、质量、适配内容；输出允许人工修改并保留版本。
- [ ] **P0 验收**：AI 预检与人工验收分开，记录通过项、问题项和退回原因。
- [ ] **P0 使用关系**：素材与方案、内容、发布任务的 `reserved/used` 关系；避免同一独占素材重复占用。
- [ ] **P0 来源指标**：商家素材、员工上传、KOC 上传、AI 衍生；KOC 公开指标与后台指标分开，缺失为 `null`。
- [ ] **P1 素材加工**：裁剪、比例转换、去水印、衍生版本；保留父子 lineage、处理参数和工具版本。
- [ ] **P1 存储治理**：用量、重复文件、未引用文件、回收站和生命周期规则。

### 8.2 素材需求与外部素材任务

- [ ] **P0 素材需求**：内容位需要什么素材、数量、规格、截止时间、是否阻塞发布。
- [ ] **P0 素材任务状态**：`unclaimed/in_progress/uploaded/ai_precheck/pending_submit/pending_acceptance/partial_reshoot/accepted/closed`。
- [ ] **P0 任务派发**：可派给内部员工；通过 TAPTIK 服务号通知，员工 H5 领取与提交。
- [ ] **P0 分镜/拍摄项**：每个 shot 独立 `pending/uploaded/rejected/completed`，支持部分退回补拍。
- [ ] **P0 验收闭环**：提交 → AI 预检 → 人工验收 → 通过入库并关联需求；退回必须说明具体项。
- [ ] **P0 起盘阶段允许素材任务**：即使尚未创建方案，也可以创建商家级素材收集任务；其 `projectId` 可为空。
- [ ] **P1 外部执行人链接**：短期链接、领取、上传、撤销、过期与审计。

接口：`/merchants/{merchantId}/materials`、`/materials/{id}`、`/projects/{projectId}/material-requirements`、`/material-tasks`、`/material-tasks/{id}/claim`、`/material-tasks/{id}/submit`、`/material-tasks/{id}/acceptance`、`/materials/{id}/derivations`。

## 9. 知识与记忆（本地资料链接）

### 9.1 来源连接

- [ ] **P0 文案和语义统一**：叫“链接本地文件/文件夹”，不是“上传知识资料”。
- [ ] **P0 本地桥接**：桌面端/本地 Agent 负责读取用户授权路径；云端后端不能保存或假装能直接访问 Windows 路径。
- [ ] **P0 来源登记**：来源类型、设备 ID、授权路径的安全标识、显示名称、文件数量、哈希、最后同步时间、状态、错误。
- [ ] **P0 来源状态**：`normal/pending/disconnected/error`；移动、删除、权限撤销后可检测。
- [ ] **P0 增量同步**：基于文件哈希/修改时间，仅解析变化文件；避免重复原子事实。
- [ ] **P0 支持格式**：PDF、Word、Excel、纯文本、Markdown；不支持格式返回明确原因。
- [ ] **P0 解析任务**：异步任务显示排队、解析、抽取、索引、完成/失败进度，可重试单文件。
- [ ] **P0 隐私边界**：明确哪些内容会被发送给模型/后端；可配置只发送摘要或选中内容。

### 9.2 原子知识与冲突处理

- [ ] **P0 类型**：商家事实、规则与禁区、项目要求、经验建议。
- [ ] **P0 状态**：`confirmed/pending/rejected/expired`；高风险规则必须人工确认。
- [ ] **P0 来源证据**：每条事实关联来源、文件、位置/页码、摘录哈希、抽取时间。
- [ ] **P0 冲突检测**：同一主题出现冲突时创建待确认任务，不自动覆盖旧事实。
- [ ] **P0 分类**：品牌、产品、账号、人设、顾客、内容、禁区、话术、素材偏好、复盘经验。
- [ ] **P0 失效与影响分析**：知识失效后标记受影响的未发布草稿/未来方案；历史内容不改写。
- [ ] **P1 经验写回**：复盘建议经人工确认后写为经验建议，记录来源复盘 ID 和适用范围。

### 9.3 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/merchants/{merchantId}/knowledge-sources` | 登记本地来源元数据 |
| `POST` | `/knowledge-sources/{sourceId}/sync` | 提交本地扫描清单并启动解析 |
| `GET` | `/knowledge-sources/{sourceId}/sync-status` | 同步进度和失败文件 |
| `DELETE` | `/knowledge-sources/{sourceId}` | 断开来源，不默认删除已确认事实 |
| `GET` | `/merchants/{merchantId}/knowledge-facts` | 搜索、类型、状态、来源筛选 |
| `PATCH` | `/knowledge-facts/{factId}` | 确认、拒绝、编辑、失效 |
| `GET` | `/merchants/{merchantId}/knowledge-tasks` | 缺资料、冲突、高风险确认 |
| `POST` | `/knowledge-tasks/{taskId}/resolve` | 处理待确认项 |

## 10. 账号资产

- [ ] **P0 账号关系**：自有品牌号、员工 KOS、协作 KOC；关系变化保留历史。
- [ ] **P0 平台授权**：通过平台官方授权/服务连接，不存储平台密码；保存授权范围、过期时间、刷新状态。
- [ ] **P0 账号档案**：平台、昵称、平台 ID、头像、矩阵角色、人设、负责人、部门、运营说明。
- [ ] **P0 员工绑定**：账号绑定员工后，员工才能在 H5 收到该账号的发布任务。
- [ ] **P0 设备绑定**：一个发布设备手机号同一时刻只能绑定一个账号；一个员工可以管理多个账号。
- [ ] **P0 采集状态**：`updated/collecting/partial/missing/failed/not_collected`，记录覆盖范围、最后采集、错误和重试。
- [ ] **P0 数据维度**：笔记、直播、粉丝、互动、账号画像；缺失字段为 `null + missingReason`。
- [ ] **P0 发布日历**：由内容位和发布任务计算，后端返回统一日历事件。
- [ ] **P0 解绑影响**：若存在待执行发布任务，解绑前返回影响清单并要求改派或取消。
- [ ] **P1 重新采集队列**：支持指定账号/数据域重采，限流、排队和失败重试。

接口：`/merchants/{merchantId}/accounts`、`/account-authorizations`、`/accounts/{id}/employee-binding`、`/accounts/{id}/device-binding`、`/accounts/{id}/collection-jobs`、`/accounts/{id}/calendar`、`/accounts/{id}/metrics`。

## 11. 执行中心

### 11.1 统一任务模型

- [ ] **P0 任务类型**：内容确认、素材收集/验收、发布执行、异常处理、人工确认；每个任务只描述一个明确动作。
- [ ] **P0 任务状态**：内部统一 `pending/in_progress/completed/cancelled`，业务子状态由具体任务对象维护。
- [ ] **P0 责任归属**：负责人、协作人、等待方、截止时间、优先级、是否阻塞、所属商家/方案/内容位。
- [ ] **P0 任务过滤**：需要我处理、团队处理中、系统处理中、已完成、全部记录；后端按当前用户权限计算。
- [ ] **P0 下一步动作**：返回允许的动作列表和禁用原因，避免前端猜测按钮是否可点。
- [ ] **P0 事实与推断分离**：异常详情分别返回已确认事实、系统推断、待确认问题和证据。
- [ ] **P0 时间线**：领取、提交、退回、重派、完成、异常、恢复均写入追加式事件。
- [ ] **P0 批量操作**：仅允许相同类型且当前状态兼容的任务；单项失败不能让批次结果不明确。
- [ ] **P1 置顶与个人关注**：属于用户级偏好，不改变团队任务优先级。

### 11.2 异常类型

后端至少识别并返回：超时未领取、素材补拍超时、发布超时、发布链接未验证、执行账号不可用、数据采集授权过期、内容合规风险。每个异常必须有 `severity`、`detectedAt`、`evidence`、`assignee`、`resolutionStatus`、`recommendedActions`。

### 11.3 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/execution-tasks` | 按商家、方案、类型、视图、状态查询 |
| `GET` | `/execution-tasks/{taskId}` | 详情、事实、证据、允许动作、时间线 |
| `POST` | `/execution-tasks/{taskId}/claim` | 领取 |
| `POST` | `/execution-tasks/{taskId}/actions` | 执行动作，带 actionType 和业务参数 |
| `POST` | `/execution-tasks:batch-action` | 批量执行，返回逐项结果 |
| `GET` | `/issues` | 异常列表 |
| `POST` | `/issues/{issueId}/resolve` | 处理结论和证据 |

## 12. 发布任务、员工 H5 与外部账号 H5

### 12.1 发布任务生成

- [ ] **P0 生成时机**：方案确认并有已确认内容后才创建可派发发布任务；方案草稿阶段不向员工发送通知。
- [ ] **P0 任务快照**：冻结标题、正文、标签、素材、目标账号、发布时间、策略版本；后续编辑必须生成新快照并通知执行人。
- [ ] **P0 两种发布链路**：
  1. 自有账号：账号资产已绑定员工，任务派给员工并在员工 H5 展示。
  2. 外部账号：针对具体发布任务生成一次性/限时二维码，扫码进入该任务的外部发布 H5。
- [ ] **P0 不能混用**：外部二维码不可成为通用项目入口；自有账号未绑定员工时任务保持阻塞并提示先绑定。

### 12.2 发布状态机

```text
unscheduled
  → pending_claim
  → preparing
  → pending_publish
  → publishing
  → link_returned
  → system_verifying
  → verified / verification_exception
  → manual_confirmed（仅异常时可用）
  → closed

任意未发布状态可在权限和原因校验后 cancelled；状态不可跳跃推进。
```

- [ ] **P0 状态迁移校验**：每次迁移验证操作者、前置状态、必填证据。
- [ ] **P0 发布回执**：发布 URL、平台内容 ID、发布时间、截图、备注；重复回传幂等。
- [ ] **P0 系统验证**：识别内容是否可访问、账号是否匹配、发布时间、内容 ID；失败进入明确异常，不直接标为已发布。
- [ ] **P0 人工确认**：只在系统无法验证时开放，需原因和截图，并记录确认人。
- [ ] **P0 已发布内容状态**：`pending_verification/verified/observing/temporarily_unavailable/manual_review/closed`。

### 12.3 员工 H5

- [ ] **P0 服务号身份绑定**：员工关注 TAPTIK 服务号，绑定组织员工身份；支持解绑和失效。
- [ ] **P0 任务通知**：创建、改派、临期、超时、退回时发送模板消息；通知失败可重试且不重复轰炸。
- [ ] **P0 H5 任务列表**：只展示本人可处理任务，按待处理/进行中/已完成筛选。
- [ ] **P0 发布助手**：复制标题/正文/标签、保存素材、打开平台 App、填写或识别发布链接。
- [ ] **P0 权限和过期**：员工离职/停用/账号解绑后立即失去访问权限。
- [ ] **P1 通知渠道扩展**：企业微信、飞书、钉钉使用同一通知适配层，保留渠道消息 ID 和投递回执。

### 12.4 外部账号 H5

- [ ] **P0 二维码令牌**：签名、过期、用途、最大使用次数、任务 ID、撤销时间。
- [ ] **P0 首次领取**：可选手机号/微信身份校验；同一任务并发领取只能成功一次。
- [ ] **P0 最小数据暴露**：只返回本任务发布所需内容与素材，不返回商家其他数据。
- [ ] **P0 提交回执**：链接、截图、备注；后台验证后关闭任务。
- [ ] **P0 重发/撤销**：二维码泄露或执行人变更时可撤销并签发新令牌，旧令牌立即失效。

### 12.5 建议接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/projects/{projectId}/publish-tasks:generate` | 从已确认内容生成任务 |
| `GET/PATCH` | `/publish-tasks/{taskId}` | 详情/改派/计划时间 |
| `POST` | `/publish-tasks/{taskId}/claim` | 领取 |
| `POST` | `/publish-tasks/{taskId}/receipt` | 回传发布证据 |
| `POST` | `/publish-tasks/{taskId}/verify` | 触发系统验证 |
| `POST` | `/publish-tasks/{taskId}/manual-confirm` | 异常人工确认 |
| `POST` | `/publish-tasks/{taskId}/external-token` | 生成外部 H5 二维码令牌 |
| `DELETE` | `/publish-tasks/{taskId}/external-token/{tokenId}` | 撤销令牌 |
| `GET` | `/staff-h5/tasks` | 当前员工任务 |
| `GET` | `/external-h5/publish/{token}` | 获取单任务最小数据 |
| `POST` | `/external-h5/publish/{token}/receipt` | 外部回传 |

## 13. 消费者/KOC 内容包领取与反馈落地页

- [ ] **P0 落地页配置**：免登录/微信登录、Banner、海报、页面 URL、问卷、有效期。
- [ ] **P0 内容包版本**：每次领取冻结 `contentPackageVersion` 和 `strategyVersionId`。
- [ ] **P0 并发领取**：领取数量、每人上限、库存扣减必须事务化和幂等。
- [ ] **P0 体验反馈**：问卷答案、图片、联系方式、同意条款；敏感信息加密和权限隔离。
- [ ] **P0 反馈生成内容**：反馈提交后异步生成个体化内容位/草稿、素材需求和后续发布任务草案。
- [ ] **P0 新版本边界**：运营人员修改内容包后只影响新领取者，不重写已领取内容。
- [ ] **P0 风险控制**：令牌过期、重复提交、防刷、文件扫描、隐私授权和删除请求。

接口：`/projects/{projectId}/landing-page-settings`、`/projects/{projectId}/content-package-versions`、`/consumer-h5/{token}/claim`、`/consumer-h5/{token}/feedback`、`/consumer-claims/{claimId}`。

## 14. 发布数据采集、搜索卡位与指标快照

- [ ] **P0 数据源登记**：明确来自平台 API、授权采集、公开页面识别还是人工录入。
- [ ] **P0 快照而非覆盖**：笔记指标按采集时间保存快照；点赞、收藏、评论、曝光等不可只存最新值。
- [ ] **P0 观察窗口**：D7/D14/D30/截至当前；后端按发布时间和时区计算到期。
- [ ] **P0 搜索卡位快照**：关键词、搜索时间、排名、结果内容、采集方式、失败原因。
- [ ] **P0 数据质量**：`complete/partial/missing/stale/failed`，附缺失字段和最后成功时间。
- [ ] **P0 授权过期**：创建异常与续权待办，不将旧数据更新为 0。
- [ ] **P1 采集调度**：频率、配额、退避、渠道限流、重试、死信和监控。
- [ ] **P1 证据快照**：对关键判断保留截图/响应摘要/哈希，便于复盘解释。

接口：`/published-notes/{id}/performance-snapshots`、`/projects/{id}/keyword-search-snapshots`、`/collection-jobs`、`/collection-jobs/{id}/retry`。

## 15. 复盘与报告

### 15.1 创建与运行条件

- [ ] **P0 禁止空复盘**：方案未发布或无可用数据时不创建复盘任务；返回缺失条件和预计可复盘时间。
- [ ] **P0 复盘范围**：方案、轮次、账号、内容、观察窗口、数据截止时间。
- [ ] **P0 方向**：搜索卡位、内容与人群、种草转化；可单选或组合。
- [ ] **P0 异步流水线**：数据校验 → 数据补充 → 分析 → 结论 → 建议 → 报告；每步可观察进度和失败原因。
- [ ] **P0 复盘状态**：`queued/analyzing/completed/exception/cancelled`。
- [ ] **P0 数据不足处理**：阻塞项、缺失数据、补充动作、可重试步骤；不得凭空生成确定结论。

### 15.2 报告与建议

- [ ] **P0 结论可追溯**：每个结论关联指标、内容、账号、时间范围和证据。
- [ ] **P0 横向对比**：同商家相似方案/历史轮次，明确可比口径。
- [ ] **P0 建议落点**：方案级、内容位级、指定笔记级、人工任务；携带预期影响和依据。
- [ ] **P0 应用建议**：应用为新的策略版本/新任务/知识候选，不修改历史数据。
- [ ] **P0 复盘调整状态**：`pending/applied/dismissed`，记录处理人、理由和生成的资源 ID。
- [ ] **P0 报告版本**：刷新最新数据应生成新版本。即便前端当前文案表示覆盖，后端仍应保留历史快照以满足审计。
- [ ] **P0 导出**：生成 PDF/表格文件的异步任务、下载授权和过期时间。
- [ ] **P1 经验沉淀**：建议经人工确认后写回知识与记忆，标明适用范围和来源。

接口：`/projects/{projectId}/reviews`、`/reviews/{reviewId}`、`/reviews/{reviewId}/retry`、`/reviews/{reviewId}/versions`、`/reviews/{reviewId}/adjustment-proposals`、`/review-adjustments/{id}/apply`、`/reviews/{id}/exports`。

## 16. 技能中心、专家与能力调用

- [ ] **P0 专家资产**：名称、定位、能力、适用范围、版本、状态、创建者。
- [ ] **P0 技能契约**：目标、适用场景、输入、输出、可执行动作、人工确认点、证据要求、失败处理、评测、版本、依赖和权限。
- [ ] **P0 生命周期**：`uninstalled/installed/enabled/needs_config/test_failed/disabled`。
- [ ] **P0 安装安全**：外部包来源、签名/哈希、依赖扫描、权限声明、测试结果；未通过不可启用。
- [ ] **P0 作用域**：任务、方案、商家、全组织；执行时再次校验当前用户和商家权限。
- [ ] **P0 运行记录**：输入快照、输出、工具调用、耗时、费用、错误、证据和人工确认。
- [ ] **P0 结果采用**：可生成策略版本、验证方案、执行待办、知识候选或经验；每种采用都必须生成明确业务资源。
- [ ] **P1 推荐能力**：区分事实、推断和缺失信息，返回准备度与所需前置资料。
- [ ] **P1 版本与评测**：灰度启用、回滚、成功率、人工采纳率、成本和延迟。

接口：`/experts`、`/skills`、`/skills/import-jobs`、`/skills/{id}/versions`、`/skills/{id}/enable`、`/skill-runs`、`/skill-runs/{id}/adopt-result`、`/capability-recommendations`。

## 17. 对话工作台与 AI 运行

- [ ] **P0 商家级会话**：对话必须归属商家；跨商家切换不泄漏历史上下文。
- [ ] **P0 消息持久化**：文本、附件引用、提及的技能/专家、时间、发送者、状态。
- [ ] **P0 附件引用**：只引用已授权素材/知识，不把本地绝对路径暴露给其他用户。
- [ ] **P0 流式响应**：中断、重试、取消、恢复；重复请求不生成两条业务动作。
- [ ] **P0 提案与执行分离**：AI 可以提出“创建方案/任务/写入知识”等提案，只有有权限的人确认后才真正写入。
- [ ] **P0 子任务记录**：子 Agent/技能运行独立留痕，可回到原消息和输入快照。
- [ ] **P0 引用证据**：回答引用具体知识事实、方案、数据快照，并标记过期/缺失。
- [ ] **P1 会话搜索和归档**：按商家、成员、日期、关键词检索。

接口：`/merchants/{merchantId}/conversations`、`/conversations/{id}/messages`、`/agent-runs`、`/agent-runs/{id}/cancel`、`/proposals/{id}/confirm`。

## 18. 通知中心与第三方渠道

- [ ] **P0 统一通知事件**：业务模块只发事件，不直接写死服务号、飞书或企微调用。
- [ ] **P0 通知模板**：按事件、渠道、语言和版本管理；变量缺失时不发送错误内容。
- [ ] **P0 收件规则**：按账号绑定、任务负责人、商家角色计算收件人。
- [ ] **P0 投递记录**：渠道消息 ID、请求时间、送达、失败、重试、已读。
- [ ] **P0 去重与降噪**：相同任务同类事件在窗口内聚合；临期提醒配置频率。
- [ ] **P0 当前渠道**：TAPTIK 服务号承担员工任务提醒与身份绑定。
- [ ] **P1 扩展渠道**：企业微信、飞书、钉钉通过适配器接入；支持组织级凭证、测试和停用。
- [ ] **P1 站内通知**：未读数、跳转资源、已读/全部已读。

接口：`/notification-connections`、`/notification-connections/{id}/test`、`/notification-preferences`、`/notifications`、`/notification-deliveries/{id}`。

## 19. 设置、计费、用量与服务管理

- [ ] **P0 个人设置**：昵称、头像、语言、时区、通知偏好、个性化和记忆开关。
- [ ] **P0 组织设置**：名称、成员、角色、权限、商家配额、集成配置。
- [ ] **P0 用量台账**：模型调用、素材处理、采集任务、存储、通知等按组织/商家/成员归集。
- [ ] **P0 配额检查**：创建或运行前校验；超额返回明确限制，不在任务运行一半后静默失败。
- [ ] **P0 账单明细**：时间、资源类型、数量、单价、金额、关联业务 ID。
- [ ] **P1 套餐与账单**：订阅、发票、支付状态、升级降级的生效边界。
- [ ] **P1 服务管理**：后台直接创建商家、邀请、停用、权限调整和集成状态检查。

接口：`/settings/me`、`/organizations/{id}/settings`、`/usage-ledger`、`/subscriptions`、`/billing-statements`。

## 20. 跨模块事件清单

后端推荐使用事务消息/Outbox，保证业务写入与事件不会一边成功一边失败。消费者必须幂等。

| 事件 | 触发条件 | 主要消费者 |
|---|---|---|
| `merchant.created` | 商家事务提交 | 起盘初始化、权限、审计 |
| `merchant.industry_changed` | 行业变更确认 | 模板迁移建议、知识检查 |
| `launch.step_changed` | 起盘步骤改变 | 下一步建议、审计 |
| `knowledge.source_linked` | 本地来源登记成功 | 解析队列、起盘进度 |
| `knowledge.fact_confirmed` | 原子事实确认 | 内容/策略上下文刷新 |
| `material.asset_available` | 素材验收通过 | 素材匹配、需求状态、起盘进度 |
| `material.task_status_changed` | 素材任务迁移 | 通知、异常、时间线 |
| `account.employee_bound` | 账号绑定员工 | 起盘进度、发布可派发性 |
| `account.authorization_expiring` | 授权临期 | 通知、续权待办 |
| `project.created` | 方案创建 | 起盘进度、审计 |
| `strategy.version_activated` | 新策略版本生效 | 后续生成上下文、审计 |
| `content.draft_confirmed` | 草稿确认 | 发布任务生成资格 |
| `publish.task_created` | 可发布任务创建 | 执行中心、通知 |
| `publish.task_claimed` | 员工/外部人员领取 | 时间线、通知 |
| `publish.receipt_submitted` | 回传链接和证据 | 验证队列 |
| `published_note_verified` | 系统/人工验证 | 观察调度、起盘步骤、复盘资格 |
| `performance.snapshot_created` | 指标采集成功 | 报告进度、异常检查 |
| `collection.failed` | 采集失败/授权过期 | 异常、通知 |
| `review.eligible` | 满足复盘条件 | 复盘建议/待办 |
| `review.completed` | 分析完成 | 报告通知、调整建议 |
| `review.adjustment_applied` | 人工采用建议 | 新策略版本/任务/知识候选 |
| `task.overdue` | 到期扫描命中 | 异常、提醒、升级 |

## 21. 权限矩阵（最低要求）

| 动作 | 组织管理员 | 商家管理员 | 操盘手 | 审核员 | 发布员工 | 外部执行人 |
|---|---:|---:|---:|---:|---:|---:|
| 创建/归档商家 | ✓ | - | - | - | - | - |
| 编辑商家档案与起盘模板 | ✓ | ✓ | 可配置 | 只读 | - | - |
| 创建/修改方案 | ✓ | ✓ | ✓ | 只读 | - | - |
| 确认内容与策略 | ✓ | ✓ | ✓ | ✓ | - | - |
| 管理素材与验收 | ✓ | ✓ | ✓ | 可配置 | 上传本人任务 | 仅本人任务 |
| 管理账号和员工绑定 | ✓ | ✓ | 可配置 | - | - | - |
| 执行发布 | 可配置 | 可配置 | 可配置 | - | 本人任务 | 单令牌任务 |
| 人工确认发布异常 | ✓ | ✓ | 可配置 | - | - | - |
| 创建/应用复盘建议 | ✓ | ✓ | ✓ | 只读 | - | - |
| 查看账单/组织用量 | ✓ | 可配置 | 仅本人/商家 | - | - | - |

后端不能依赖前端隐藏按钮实现权限；每个接口都要执行资源级鉴权。

## 22. 关键状态机汇总

### 商家起盘

`new → in_progress → completed`；可从 `new/in_progress → skipped`。恢复时从 `skipped → in_progress`，必须保留首次跳过记录。

### 素材任务

`unclaimed → in_progress → uploaded → ai_precheck → pending_acceptance → accepted → closed`；验收可进入 `partial_reshoot → uploaded`。取消需记录原因。

### 内容草稿

`pending_generation → generated → pending_confirmation → confirmed`；`pending_confirmation → rejected → pending_generation`。确认后的修改创建新版本并重新确认。

### 发布任务

见第 12 节。验证失败不是终态；进入 `verification_exception` 后可重新验证或人工确认。

### 复盘

`queued → analyzing → completed`；任一步异常进入 `exception`，重试从失败步骤继续；不丢弃已完成数据快照。

## 23. 异步任务与失败恢复

- [ ] 每个异步任务返回阶段、百分比、当前对象、开始/结束时间、可否取消、错误和重试建议。
- [ ] 相同输入快照和幂等键不会重复生成资源。
- [ ] 部分成功必须列出成功/失败对象；重试只处理失败部分。
- [ ] 任务执行器崩溃可继续，不把 `running` 永久挂住。
- [ ] 超时、限流和第三方不可用有指数退避和死信记录。
- [ ] 用户取消只停止未完成步骤，已创建业务资源按补偿规则处理并留审计。
- [ ] 前端刷新或换设备后仍能查询原任务进度。

## 24. 数据与文件安全

- [ ] 手机号、微信身份、联系方式、授权令牌加密存储，日志脱敏。
- [ ] 文件上传使用短期预签名地址；限制类型、大小，做恶意文件扫描。
- [ ] 外部 H5 令牌不可出现在长期日志或分析埋点明文中。
- [ ] 本地知识路径只保留安全化显示/设备侧引用，不能让其他设备通过路径访问。
- [ ] 商家删除/导出满足数据保留策略；关联发布与审计记录按合规要求保留或匿名化。
- [ ] 所有第三方凭证放密钥管理，不进入前端包或普通数据库字段。

## 25. 前端 Mock 替换对照

| 当前前端区域 | 当前行为 | 后端接入后必须替换为 |
|---|---|---|
| `App.tsx` 商家列表 | React 本地 state 新增/切换 | 商家查询、创建、权限与起盘状态 API |
| `CreateMerchantModal.tsx` | 本地生成商家 ID/行业默认值/二维码 | 行业字典、事务创建、邀请令牌 API |
| `NewMerchantLaunchGuide.tsx` | `localStorage` 保存模板和进度 | 商家起盘模板、进度、依赖计算 API |
| `ProjectContext.tsx` | 内存中创建方案、内容、任务、时间线 | 方案聚合、策略版本、工作流与事件 API |
| `projectStore.ts` / `unifiedStore.ts` | 两套 Mock 模型并派生 | 以后端规范 DTO 为唯一事实源；前端仅适配展示 |
| `MaterialCenter` / `materialMockData.ts` | 静态素材和任务 | 素材、文件、分析、任务、验收 API |
| `KnowledgeMemory` / `knowledgeMockData.ts` | 静态知识、本地选择器 | 本地桥接 + 来源登记/同步/知识事实 API |
| `AccountAssetsV2` | 静态账号、采集、日历 | 平台授权、员工/设备绑定、采集和日历 API |
| `ExecutionCenter` / `mockData.ts` | 静态执行任务和异常 | 统一任务查询、动作、异常、时间线 API |
| `StaffH5PublishModal.tsx` | 模拟员工发布步骤 | 员工身份、任务、回执、验证和通知 API |
| 消费者落地页 | 本地内容包和问卷 | 领取库存、版本冻结、反馈与个体内容生成 API |
| `ReviewReport` / `reviewMockData.ts` | 静态报告和本地动作 | 数据快照、异步分析、版本、建议应用、导出 API |
| `SkillsCenter` | 静态专家/技能状态 | 技能包、权限、运行、测试、版本和审计 API |
| 新对话/工作台 | 本地消息和模拟 Agent | 会话、流式响应、运行、提案确认 API |

## 26. 推荐交付顺序

1. **基础层 P0**：身份组织、商家隔离、统一错误/分页/审计、文件存储、异步任务。
2. **新商家闭环 P0**：行业字典 → 创建商家 → 起盘模板/进度 → 商家档案。
3. **资源准备 P0**：本地知识来源 → 素材资产/素材任务 → 账号资产/员工绑定。
4. **方案闭环 P0**：方案 → 策略版本 → 内容位/草稿 → 素材匹配 → 发布任务。
5. **外部执行 P0**：服务号员工身份与 H5、自有账号任务、外部账号二维码 H5、发布回执与验证。
6. **数据复盘 P0**：指标快照 → 观察窗口 → 复盘报告 → 建议应用为新版本。
7. **智能化与治理**：技能/专家、对话工作台、通知扩展、计费、组织模板和运营监控。

## 27. 联调与上线 Definition of Done

每个模块上线前逐项确认：

- [ ] OpenAPI/接口文档包含请求、响应、枚举、错误码和权限。
- [ ] 数据库迁移、唯一约束、外键/软删除和索引完成。
- [ ] 所有创建接口通过重复点击/网络重试的幂等测试。
- [ ] 所有状态迁移覆盖合法、非法、并发三类测试。
- [ ] 刷新、退出重登、换浏览器后状态一致。
- [ ] 商家 A 成员无法通过 ID 访问商家 B 数据。
- [ ] 异步任务支持进度、失败、重试、取消和告警。
- [ ] 通知失败不会回滚核心业务，且可补发、可去重。
- [ ] 缺失指标显示“暂无/未采集/授权失效”，不显示 0。
- [ ] 所有 AI 生成结果记录输入快照、模型/版本和人工采用情况。
- [ ] 发布、验收、人工确认、复盘建议应用均有证据和审计。
- [ ] 前端已删除对应 Mock 写入路径，服务端响应成为唯一事实源。
- [ ] 测试环境有一套贯穿全链路的数据：新商家 → 起盘 → 方案 → 员工/外部发布 → 回执 → 数据 → 复盘。

## 28. 仍需产品明确的决策

这些问题不阻塞后端搭骨架，但必须在对应模块开发前定稿：

1. 新商家负责人账号是创建 TAPTIK 用户，还是仅作为商家联系人？初始密码是否保留。
2. 行业默认模板由谁维护，按组织共用还是平台统一，是否需要发布审核。
3. 起盘“手动完成”是否允许绕过真实资源条件，还是只能标记“了解/稍后处理”。
4. 本地知识正文的云端边界：完整文本、分块摘要、向量，还是仅按需临时传输。
5. 小红书等平台的数据获取与发布验证使用哪些正式接口/授权方式，合法采集边界是什么。
6. 员工 H5 当前服务号主体、身份绑定方式、模板消息能力和域名备案情况。
7. 外部发布任务是否需要实名/手机号验证，二维码默认有效期和领取次数。
8. 发布链接无法验证时，哪些角色有人工确认权限，人工确认是否影响复盘可信度标记。
9. 复盘数据最小门槛、默认观察周期和“数据不足仍生成报告”的展示口径。
10. 素材原文件、发布截图、数据证据和对话的保留/删除期限。

---

建议后端把本文件拆成 Epic 和接口任务时，保留原章节编号。例如 `BE-12.4-03 外部 H5 最小数据暴露`，前端和测试即可直接使用同一编号验收，避免功能在转交过程中遗漏。
